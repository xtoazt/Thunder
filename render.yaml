services:
  - type: web
    name: thundr
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 8080
    autoDeploy: true
    healthCheckPath: /
    disk:
      name: thundr-data
      mountPath: /data
      sizeGB: 1
